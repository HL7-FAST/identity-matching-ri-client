<%= form_with(model: authority) do |form| %>
  <% if authority.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(authority.errors.count, "error") %> prohibited this authority from being saved:</h2>

      <ul>
        <% authority.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :certificate, style: "display: block" %>
    <%= form.file_field :certificate %>
  </div>

  <div>
    <%= form.label :private_key, style: "display: block" %>
    <%= form.file_field :private_key %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
