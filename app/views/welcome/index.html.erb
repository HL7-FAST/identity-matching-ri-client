<h1 class="w-100 text-center">Identity Matching RI Client</h1>

<p class="lead">This is a client reference implementation conforming to the <a href="https://build.fhir.org/ig/HL7/fhir-identity-matching-ig/index.html">FHIR Identity Matching IG</a> by the FHIR at Scale Taskforce (FAST).</p>

<p class="lead">Please enter a conforming server implementation to continue:</p>

<%= form_for :patient_server, url: "/patient_servers", id: "patient_server_form" do |f| %>
    <div class="row justify-content-center">
		<div class="col-md-8 mb-3">
			<%= f.text_field :base, placeholder: "Enter Base URL. Ex: http://myserver.com/fhir/", class: "form-control", size: 64, required: true %>
		</div>
	</div>
	<div class="row row-cols-md-auto g-3 justify-content-center">
		<div class="col-auto">
			<%= f.submit "Metadata", class: "btn btn-warning" %>
		</div>
		<div class="col-auto">
			<%= f.submit "Register (UDAP)", class: "btn btn-dark" %>
		</div>
		<div class="col-auto">
			<%= f.submit "Authenticate (OAuth2)", class: "btn btn-danger", onclick: "alert('OAuth2 Not Implemented'); return false;" %>
		</div>
		<div class="col-auto">
			<%= f.submit "Match Patient", class: "btn btn-primary" %>
		</div>
	</div>
<% end %>
